package com.mani.programes;

public class JEP382 {

    /**
     * This JEP implements a Java 2D internal rendering pipeline for macOS as Apple
     * deprecated the OpenGL API (in macOS 10.14), used internally in Swing GUI.
     * The new implementation uses the Apple Metal API,
     * and apart from the internal engine, there were no changes to the existing APIs.
     */
}
